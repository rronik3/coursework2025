    <Window x:Class="coursework.MainWindow"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:local="clr-namespace:coursework"
            xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
            Style="{StaticResource MaterialDesignWindow}"
            WindowStartupLocation="CenterScreen"
            mc:Ignorable="d"
            Title="Кулінарні рецепти" Height="450" Width="800">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Панель з кнопками -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Button Name="AddButton" Content="Додати" Width="100" Margin="5" Click="AddButton_Click"/>
                <Button Name="EditButton" Content="Редагувати" Width="105" Margin="5" Click="EditButton_Click"/>
                <Button Name="DeleteButton" Content="Видалити" Width="100" Margin="5" Click="DeleteButton_Click"/>
                <TextBox Name="SearchBox" Width="200" Margin="10,0,5,0"
                         materialDesign:HintAssist.Hint="Пошук..."/>
                <Button Name="SearchButton" Content="Шукати" Width="100" Click="SearchButton_Click"/>
                <Button Content="Вийти" Width="100" Margin="5" Click="LogoutButton_Click"/>
            </StackPanel>

            <!-- Таблиця з рецептами -->
            <DataGrid Name="RecipesDataGrid" 
                      Grid.Row="1"
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      MouseDoubleClick="RecipesDataGrid_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Назва" Binding="{Binding Title}" Width="*"/>
                    <DataGridTextColumn Header="Категорія" Binding="{Binding Category}" Width="*"/>
                    <DataGridTextColumn Header="Час приготування" Binding="{Binding CookingTime}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Window>
